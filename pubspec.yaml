name: relaunch_app
description: none
version: 0.0.1
homepage: https://github.com/subhashPetro/relaunch_app_plugin

environment:
  sdk: '>=3.0.1 <4.0.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

  plugin:
    platforms:
      android:
        package: com.example.relaunch_app
        pluginClass: RelaunchAppPlugin

